# System Overview

WokAPI is the shared infrastructure API for the WokSpec ecosystem.

It provides:

- authentication
- sessions
- billing
- shared API routing
- product registry behavior

It should be treated as high-impact infrastructure rather than a product-specific application surface.
